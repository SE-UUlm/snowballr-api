syntax = "proto3";
package snowballr;

enum ExportFormat {
  EXPORT_FORMAT_UNSPECIFIED = 0;
  EXPORT_FORMAT_CSV = 1;
  EXPORT_FORMAT_ZIP = 2;
}

message ExportRequest {
  string id = 1;
  ExportFormat format = 2;
}
