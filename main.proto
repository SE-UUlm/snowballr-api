syntax = "proto3";

service Authenticator {
  rpc Login (LoginRequest) returns (LoginReply);
}

message LoginRequest {
  string email = 1;
  string password = 2;
}

message LoginReply {
  string jwt = 1;
}
