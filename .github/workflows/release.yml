name: Release
on:
    push:
        tags: ["v*.*.*"]

jobs:
    release:
        name: Release
        if: github.ref_type == 'tag'
        permissions:
            contents: write
        uses: SE-UUlm/snowballr-ci/.github/workflows/release.yml@feat/1-feature-add-initial-workflows
